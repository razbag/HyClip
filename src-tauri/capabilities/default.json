{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the HayClip popup window",
  "windows": ["popup", "preferences"],
  "permissions": ["core:default"]
}
